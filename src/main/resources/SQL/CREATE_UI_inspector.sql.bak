DROP TABLE IF EXISTS `TB_PRJT_INFO`;
CREATE TABLE `TB_PRJT_INFO` (
	`PRJT_CD`	VARCHAR(20)	NOT NULL PRIMARY KEY COMMENT '프로젝트코드',
	`PRJT_NM`	VARCHAR(200)	NULL COMMENT '프로젝트명',
	`CUST_NM`	VARCHAR(100)	NULL COMMENT '고객명',
	`PM_NM`	VARCHAR(60)	NULL COMMENT 'PM명',
	`STRT_DT`	DATETIME	NULL COMMENT '시작일자',
	`END_DT`	DATETIME	NULL COMMENT '종료일자',
	`MT_USE_YN`	VARCHAR(1)	NULL COMMENT '메타사용여부',
	`FRST_CRTR_ID`	VARCHAR(50)	NOT NULL COMMENT '최초생성자ID',
	`FRST_CRDT`	DATETIME	NOT NULL COMMENT '최초생성일시',
	`LST_MDFR_ID`	VARCHAR(50)	NOT NULL COMMENT '최종수정자ID',
	`LST_MDDT`	DATETIME	NOT NULL COMMENT '최종수정일시'
) COMMENT = '프로젝트정보';

DROP TABLE IF EXISTS TB_UI_MNG;
CREATE TABLE `TB_UI_MNG` (
  `PRJT_CD`	VARCHAR(20)	NOT NULL COMMENT '프로젝트코드',
	`UI_CD`	VARCHAR(2)	NOT NULL COMMENT 'UI코드',	
	`UI_NM`	VARCHAR(20)	NULL COMMENT 'UI명',
	`USE_YN`	VARCHAR(1)	NOT NULL COMMENT '사용여부',
	`FRST_CRTR_ID`	VARCHAR(50)	NOT NULL COMMENT '최초생성자ID',
	`FRST_CRDT`	DATETIME	NOT NULL COMMENT '최초생성일시',
	`LST_MDFR_ID`	VARCHAR(50)	NOT NULL COMMENT '최종수정자ID',
	`LST_MDDT`	DATETIME	NOT NULL COMMENT '최종수정일시',
	PRIMARY KEY (PRJT_CD,UI_CD)
) COMMENT = 'UI관리';

DROP TABLE IF EXISTS TB_UI_CMPN_MNG;
CREATE TABLE `TB_UI_CMPN_MNG` (
  `PRJT_CD`	VARCHAR(20)	NOT NULL COMMENT '프로젝트코드',
  `UI_CD`	VARCHAR(2)	NOT NULL COMMENT 'UI코드',
	`CMPN_CD`	VARCHAR(2)	NOT NULL COMMENT '컴포넌트코드',	
	`CMPN_NM`	VARCHAR(100)	NULL COMMENT '컴포넌트명',
	`USE_YN`	VARCHAR(1)	NOT NULL COMMENT '사용여부',
	`FRST_CRTR_ID`	VARCHAR(50)	NOT NULL COMMENT '최초생성자ID',
	`FRST_CRDT`	DATETIME	NOT NULL COMMENT '최초생성일시',
	`LST_MDFR_ID`	VARCHAR(50)	NOT NULL COMMENT '최종수정자ID',
	`LST_MDDT`	DATETIME	NOT NULL COMMENT '최종수정일시',
	PRIMARY KEY (PRJT_CD,UI_CD,CMPN_CD)
) COMMENT = 'UI컴포넌트관리';

DROP TABLE IF EXISTS TB_CMPN_PRPTY_MNG;
CREATE TABLE `TB_CMPN_PRPTY_MNG` (
  `PRJT_CD`	VARCHAR(20)	NOT NULL COMMENT '프로젝트코드',
  `UI_CD`	VARCHAR(2)	NOT NULL COMMENT 'UI코드',
  `CMPN_CD`	VARCHAR(2)	NOT NULL COMMENT '컴포넌트코드',
	`PRPTY`	VARCHAR(30)	NOT NULL COMMENT '프로퍼티',
	`CHK_YN`	VARCHAR(1)	NOT NULL COMMENT '체크여부',
	`PRPTY_EXPL`	VARCHAR(300)	NULL COMMENT '프로퍼티설명',
	`DFLT_VL`	VARCHAR(10)	NULL COMMENT '디폴트값',
	`PRPTY_RMRK`	VARCHAR(1000)	NULL COMMENT '프로퍼티비고',
	`FRST_CRTR_ID`	VARCHAR(50)	NOT NULL COMMENT '최초생성자ID',
	`FRST_CRDT`	DATETIME	NOT NULL COMMENT '최초생성일시',
	`LST_MDFR_ID`	VARCHAR(50)	NOT NULL COMMENT '최종수정자ID',
	`LST_MDDT`	DATETIME	NOT NULL COMMENT '최종수정일시',
	PRIMARY KEY (PRJT_CD,UI_CD,CMPN_CD,PRPTY)
) COMMENT = '컴포넌트프로퍼티관리';

DROP TABLE IF EXISTS `TB_VLDT_RSLT_HST`;
CREATE TABLE `TB_VLDT_RSLT_HST` (
	`SEQ`	INT AUTO_INCREMENT PRIMARY KEY COMMENT '순번',
	`PRJT_CD`	VARCHAR(20)	NOT NULL COMMENT '프로젝트코드',
  `UI_CD`	VARCHAR(2)	NOT NULL COMMENT 'UI코드',
  `CMPN_CD`	VARCHAR(2)	NOT NULL COMMENT '컴포넌트코드',
	`PRPTY`	VARCHAR(30)	NOT NULL COMMENT '프로퍼티',
	`VLDT_SBJ_TERM_CD`	VARCHAR(30)	NOT NULL COMMENT '검사대상용어코드',
	`VLDT_RULE_CD`	VARCHAR(30)	NOT NULL COMMENT '검사규칙코드',
	`VLDT_DT`	VARCHAR(88)	NULL COMMENT '검사일자',
	`ITRT_SEQ`	INT	NULL COMMENT '차수',
	`FL_ID`	VARCHAR(300)	NULL COMMENT '파일ID',
	`PRGM_CMPN_ID`	VARCHAR(50)	NULL COMMENT '프로그램컴포넌트ID',
	`PRPTY_VL`	VARCHAR(100)	NULL COMMENT '프로퍼티값',
	`DFLT_VL`	VARCHAR(10)	NULL COMMENT '디폴트값',
	`PRGM_VL`	VARCHAR(100)	NULL COMMENT '프로그램값',
	`VLDT_RST_YN`	VARCHAR(1)	NULL COMMENT '검사결과여부',	
	`RMRK`	VARCHAR(1000)	NULL COMMENT '비고',
	`FRST_CRTR_ID`	VARCHAR(50)	NOT NULL COMMENT '최초생성자ID',
	`FRST_CRDT`	DATETIME	NOT NULL COMMENT '최초생성일시',
	`LST_MDFR_ID`	VARCHAR(50)	NOT NULL COMMENT '최종수정자ID',
	`LST_MDDT`	DATETIME	NOT NULL COMMENT '최종수정일시'
	CREATE INDEX IDX_VLDT_RSLT_HST_01 ( VLDT_DT );
) COMMENT = '검사결과이력';


DROP TABLE IF EXISTS `TB_VLDT_RULE_TERM_MP`;
CREATE TABLE `TB_VLDT_RULE_TERM_MP` (
	`VLDT_SBJ_TERM_CD`	VARCHAR(30)	NOT NULL COMMENT '검사대상용어코드',
	`VLDT_RULE_CD`	VARCHAR(30)	NOT NULL COMMENT '검사규칙코드',
	`USE_YN`	VARCHAR(1)	NULL COMMENT '사용여부',
	`FRST_CRTR_ID`	VARCHAR(50)	NOT NULL COMMENT '최초생성자ID',
	`FRST_CRDT`	DATETIME	NOT NULL COMMENT '최초생성일시',
	`LST_MDFR_ID`	VARCHAR(50)	NOT NULL COMMENT '최종수정자ID',
	`LST_MDDT`	DATETIME	NOT NULL COMMENT '최종수정일시',
	PRIMARY KEY (VLDT_SBJ_TERM_CD, VLDT_RULE_CD)
) COMMENT = '검사규칙용어맵핑';

DROP TABLE IF EXISTS `TB_VLDT_TERM_MSTR`;
CREATE TABLE `TB_VLDT_TERM_MSTR` (
	`VLDT_SBJ_TERM_CD`	VARCHAR(30)	NOT NULL COMMENT '검사대상용어코드',	
	`VLDT_SBJ_TERM_NM`	VARCHAR(200)	NOT NULL COMMENT '검사대상용어명',
	`EVNT_NM`	VARCHAR(200)	NULL COMMENT '이벤트명',
	`USE_YN`	VARCHAR(1)	NULL COMMENT '사용여부',
	`RMRK`	VARCHAR(1000)	NULL COMMENT '비고',
	`FRST_CRTR_ID`	VARCHAR(50)	NOT NULL COMMENT '최초생성자ID',
	`FRST_CRDT`	DATETIME	NOT NULL COMMENT '최초생성일시',
	`LST_MDFR_ID`	VARCHAR(50)	NOT NULL COMMENT '최종수정자ID',
	`LST_MDDT`	DATETIME	NOT NULL COMMENT '최종수정일시',
	PRIMARY KEY (VLDT_SBJ_TERM_CD)
) COMMENT = '검사대상용어마스터';

DROP TABLE IF EXISTS `TB_VLDT_RULE_MSTR`;
CREATE TABLE `TB_VLDT_RULE_MSTR` (
	`VLDT_RULE_CD`	VARCHAR(30)	NOT NULL COMMENT '검사규칙코드',
	`VLDT_RULE_NM`	VARCHAR(100)	NULL COMMENT '검사규칙명',
	`USE_YN`	VARCHAR(1)	NULL COMMENT '사용여부',
	`PRJT_CD`	VARCHAR(20)	NOT NULL COMMENT '프로젝트코드',
  `UI_CD`	VARCHAR(2)	NOT NULL COMMENT 'UI코드',
  `CMPN_CD`	VARCHAR(2)	NOT NULL COMMENT '컴포넌트코드',
	`PRPTY`	VARCHAR(30)	NOT NULL COMMENT '프로퍼티',
	`PRPTY_VL`	VARCHAR(100)	NOT NULL COMMENT '프로퍼티값',
	`SCRP_CNTN`	VARCHAR(1000)	NULL COMMENT '스크립트내용',
	`FRST_CRTR_ID`	VARCHAR(50)	NOT NULL COMMENT '최초생성자ID',
	`FRST_CRDT`	DATETIME	NOT NULL COMMENT '최초생성일시',
	`LST_MDFR_ID`	VARCHAR(50)	NOT NULL COMMENT '최종수정자ID',
	`LST_MDDT`	DATETIME	NOT NULL COMMENT '최종수정일시',
	PRIMARY KEY (VLDT_RULE_CD)
) COMMENT = '검사규칙마스터';